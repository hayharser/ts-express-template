export interface UserWebModel {
    _id: string;
    firstName: string;
    lastName: string;
    email: string;
    username?: string;
    phoneNumber?: string;
    profileUrl?: string;
    bio?: string;
}
